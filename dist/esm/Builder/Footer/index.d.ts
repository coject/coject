import { FC, ReactNode } from "react";
interface iFooter {
    menus?: any;
    setMenus?: any;
    copyRight?: ReactNode;
    versionName?: ReactNode;
}
export declare const Footer: FC<iFooter>;
export {};
